version: "3.9"

services:
  ai-agent:
    build: .
    container_name: ai_git_monitoring_agent

    ports:
      - "8001:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped